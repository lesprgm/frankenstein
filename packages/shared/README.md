# Handoff Shared Types

Shared TypeScript types and interfaces used by both frontend and backend.

## Usage

```typescript
import type { User, Workspace, Memory } from '@handoff/shared'
```

## Development

```bash
# Build types
npm run build:shared

# Watch mode
npm run dev --workspace=@handoff/shared
```
